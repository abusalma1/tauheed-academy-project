RewriteEngine On

# If the request is NOT a real file
RewriteCond %{REQUEST_FILENAME} !-f

# And NOT a real directory
RewriteCond %{REQUEST_FILENAME} !-d

# Send everything to index.php (your router)
RewriteRule ^ index.php [L]
